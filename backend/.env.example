# The base URL of the Rmoni API (no trailing slash)
RMONI_API_BASE_URL=https://service.rmoniweb.rmoni.com

# Your Bearer token for authentication
RMONI_API_TOKEN=your-api-token-here

# Port the Express server will listen on (optional, defaults to 3000)
PORT=3000
